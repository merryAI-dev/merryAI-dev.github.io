{
  "name": "mysc-native-editor",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/vendor.mjs",
    "check": "npm test && npm run build",
    "deploy": "npm run build && vercel --prod",
    "test": "node --test"
  },
  "dependencies": {
    "dompurify": "^3.4.12",
    "marked": "^18.0.7",
    "sanitize-html": "^2.17.6",
    "yaml": "^2.9.0"
  }
}
