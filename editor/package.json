{
  "name": "mysc-native-editor",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/vendor.mjs",
    "check": "npm test && npm run build && WRANGLER_WRITE_LOGS=false wrangler deploy --dry-run",
    "dev": "npm run build && WRANGLER_WRITE_LOGS=false wrangler dev",
    "deploy": "npm run build && WRANGLER_WRITE_LOGS=false wrangler deploy",
    "test": "node --test"
  },
  "dependencies": {
    "dompurify": "^3.4.12",
    "marked": "^18.0.7",
    "sanitize-html": "^2.17.6",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "wrangler": "4.113.0"
  },
  "overrides": {
    "sharp": "0.35.3"
  }
}
